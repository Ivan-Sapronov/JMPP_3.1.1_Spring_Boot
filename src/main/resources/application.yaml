server:
  port: 8189
  servlet:
    context-path: /
spring:
#  mvc:
#    view:
#      prefix: /views
#      suffix: .html
  jpa:
   show-sql: true
   properties:
    hibernate:
     dialect: org.hibernate.dialect.MySQL8Dialect
#     temp:
#      use_jdbc_metadata_defaults: false
  datasource:
   driver-class-name: com.mysql.cj.jdbc.Driver
   url: jdbc:mysql://localhost:3306/my_db?verifyServerCertificate=false&useSSL=false&requireSSL=false&useLegacyDatetimeCode=false&amp&serverTimezone=UTC
   username: bestuser
   password: bestuser